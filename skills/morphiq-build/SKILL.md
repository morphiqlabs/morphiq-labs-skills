---
name: Morphiq Build
description: This skill should be used when the user asks to "fix the issues", "optimize existing content", "create new content", "run Morphiq Build", or mentions building content fixes, generating schema, or creating policy files. Consumes a Prioritized Roadmap (or user prompt, or existing content) and produces build artifacts through a 5-step content lab pipeline.
version: 0.1.0
---

## Pipeline Position
Step 3 of 4 — consumes morphiq-rank output.
Input: Prioritized Roadmap (JSON) OR user prompt OR existing content.
Output: Build Output (JSON + artifacts) → consumed by morphiq-track.

<!-- TODO: Workflow instructions, content lab pipeline steps, entry point routing, reference file pointers -->
